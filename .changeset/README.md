# Changesets

Run `pnpm changeset` for any change that should publish a new version of `@tevm/logger`.

Commit the generated Markdown file with the change. The release workflow will collect pending changesets into a release
pull request and publish from that pull request after it is merged.
